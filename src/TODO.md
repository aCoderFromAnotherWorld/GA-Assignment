# TODOs

## Common Rules

- All code (including inputs and outputs) will go inside this 'src' folder
- If anyone wanna maintain any virtual environment, add the folder to '.gitignore', so we don't need to push the libraries/packages on GitHub.

## Works ToDo

- [x] Implement the 2D Ackley function (`ackley.py`)
- [x] Implement the GA engine — roulette wheel selection, 1-point crossover,
      1-point Gaussian mutation, elitism (`genetic_algorithm.py`)
- [x] Run script producing convergence log, plots, and a JSON summary (`main.py`)
- [x] Unit tests (`test_ga.py`)
- [x] Classical-optimizer comparison + reduced-generation (20/25) GA runs
- [ ] Prepare the 8-minute group presentation / exam-ready explanation of each GA step
- [ ] (Optional) Tune `mutation_sigma` / re-run with different seeds to compare convergence speed
